
server:
  port: 18080

spring:
  application:
    name: gateway-server

management:
  endpoints:
    web:
      exposure:
        include: "*"

logging:
  level:
    org.springframework.cloud.gateway: debug